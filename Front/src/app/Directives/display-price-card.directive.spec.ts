import { DisplayPriceCardDirective } from './display-price-card.directive';

describe('DisplayPriceCardDirective', () => {
  it('should create an instance', () => {
    const directive = new DisplayPriceCardDirective();
    expect(directive).toBeTruthy();
  });
});
