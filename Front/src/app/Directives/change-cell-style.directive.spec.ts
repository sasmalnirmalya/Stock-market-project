import { ChangeCellStyleDirective } from './change-cell-style.directive';

describe('ChangeCellStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeCellStyleDirective();
    expect(directive).toBeTruthy();
  });
});
