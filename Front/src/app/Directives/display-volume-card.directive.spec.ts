import { DisplayVolumeCardDirective } from './display-volume-card.directive';

describe('DisplayVolumeCardDirective', () => {
  it('should create an instance', () => {
    const directive = new DisplayVolumeCardDirective();
    expect(directive).toBeTruthy();
  });
});
