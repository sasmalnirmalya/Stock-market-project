import { DisplayRatioCardDirective } from './display-ratio-card.directive';

describe('DisplayRatioCardDirective', () => {
  it('should create an instance', () => {
    const directive = new DisplayRatioCardDirective();
    expect(directive).toBeTruthy();
  });
});
